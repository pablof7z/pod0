---
phase: 01-headless-host-crates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
autonomous: true
requirements: [HOST-04]
gap_closure: true

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A test proves Handle::block_on driven from a different thread than the one that built the Handle's Runtime actually completes (does not hang) — the exact multi-thread-vs-current-thread pitfall 01-02-SUMMARY.md documented and fixed for pod0-cli/pod0-portable-media"
  artifacts:
  key_links:
---

<objective>



</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-headless-host-crates/01-VERIFICATION.md
@.planning/phases/01-headless-host-crates/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <read_first>
    .planning/phases/01-headless-host-crates/01-02-SUMMARY.md's "Deviations from Plan" section, Deviation 1 — the exact documented pitfall this task must not repeat: a `current_thread`-flavored `Runtime`'s I/O/timer driver only makes progress on the thread that built it; `Handle::block_on` called from any other thread hangs forever. `pod0-portable-media`'s `MediaLoader::new_with_handle` (rust/crates/pod0-portable-media/src/source.rs) is the established, already-shipped pattern for the additive-constructor half of this same fix — mirror its shape: keep the existing owned-`Runtime` constructor unchanged for existing standalone callers, add a second constructor that takes a `Handle` and drives `Handle::block_on` instead.
  </read_first>
  <behavior>
  </behavior>
  <action>
  </action>
  <verify>
  </verify>
  <acceptance_criteria>
    - The new cross-thread test genuinely spawns a second OS thread and calls `Handle::block_on` from it (not from the thread that built the `Runtime`) — grep the test body for `std::thread::spawn` to confirm
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | No new dependency introduced | low | accept | This task adds no new crate dependency; `tokio::runtime::Handle` is already a dependency of this crate via `tokio::runtime::Runtime`. |

</threat_model>

<verification>
- The stash-based clean-checkout workspace build in the task's acceptance criteria exits 0
</verification>

<success_criteria>
</success_criteria>

<output>
Create `.planning/phases/01-headless-host-crates/01-05-SUMMARY.md` when done
</output>
